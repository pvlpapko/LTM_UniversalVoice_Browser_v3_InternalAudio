plugins {
    id 'com.android.application'
}

android {
    namespace 'com.ltm.universalvoicebrowser'
    compileSdk 35

    defaultConfig {
        applicationId 'com.ltm.universalvoicebrowser'
        minSdk 29
        targetSdk 35
        versionCode 4
        versionName '3.0.1-internal-audio-small-apk'

        // APK/AAB не будет тащить x86/x86_64 библиотеки GeckoView.
        // Для реальных телефонов Android обычно нужны только ARM-варианты.
        ndk {
            abiFilters 'arm64-v8a', 'armeabi-v7a'
        }
    }

    // Делает отдельные APK под ABI вместо одного огромного universal APK.
    splits {
        abi {
            enable true
            reset()
            include 'arm64-v8a', 'armeabi-v7a'
            universalApk false
        }
    }

    // Сжимает native .so внутри APK. Для GeckoView это сильно уменьшает размер APK-файла.
    packaging {
        jniLibs {
            useLegacyPackaging = true
        }
    }

    buildTypes {
        debug {
            applicationIdSuffix '.debug'
            versionNameSuffix '-debug'
            minifyEnabled false
            shrinkResources false
        }
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation "org.mozilla.geckoview:geckoview:${GECKOVIEW_VERSION}"
    implementation "com.google.mlkit:translate:${MLKIT_TRANSLATE_VERSION}"
    implementation "com.squareup.okhttp3:okhttp:${OKHTTP_VERSION}"
}
